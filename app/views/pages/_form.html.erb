<%= form_with(model: page) do |form| %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
        <% page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :list %>
    <%= form.text_field :list %>
  </div>

  <div class="field">
    <%= form.label :performance %>
    <%= form.number_field :performance %>
  </div>

  <div class="field">
    <%= form.label :check %>
    <%= form.check_box :check %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
